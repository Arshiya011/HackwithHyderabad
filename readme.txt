To get the output make sure to open a new terminal;
then select cmd(command prompt);
type the following statements:
pip install flask //to install flask
pip install requests //to install requests
python app.py //to run the file.

after this a URL will appear on the cmd,
paste it in the browser.